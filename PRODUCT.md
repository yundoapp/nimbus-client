# 云渡客户端产品上下文

## 产品定位

云渡（Yundo）是一款面向个人自用和小范围朋友使用的跨平台连接工具。内部研发代号为 Nimbus，用户可见品牌统一使用 Yundo / 云渡。

中文界面的软件名显示为 `Yundo · 云渡`，繁体中文显示为 `Yundo · 雲渡`；其他语言只显示 `Yundo`。

## 目标用户

- 不熟悉网络协议和代理工具的普通用户。
- 希望通过简单、可靠的一键加速获得稳定访问体验的个人用户。
- 需要清晰了解加速状态、节点地区、套餐流量、设备和激活状态的用户。

## 产品目标

隐藏 VPN、Proxy、Clash、节点、协议、订阅、TUN 等技术细节，让用户只需理解加速、停止加速、节点地区、加速模式、套餐、流量、设备和激活。“加速”表示让原本受限的访问变得可用，不承诺原本可访问的内容一定更快；内部 API、连接方案和会话模型继续沿用连接术语。

## 品牌气质

简洁、可靠、克制。界面表达平静、清楚，让技术退到体验背后。

## 反例

- 不做面向普通用户的网络技术控制台。
- 不堆砌协议、节点和路由术语。
- 不使用营销化、夸张或制造焦虑的表达。
- 不把相关能力拆散到多个难以关联的设置入口。

## 设计原则

- 优先解释用户行为和结果，再考虑内部技术概念。
- 使用渐进披露，把常用操作放在最相关的场景中。
- 首页不使用固定顶部栏；当前账号识别放在可滚动内容右上角，不重复展示应用图标、品牌名和版本号；首页套餐区域不重复展示可从有效期推断的时间信息。
- 登录和注册页面只展示 App 名称，不再展示解释性副标题，也不区分开发版和正式版；开发版识别保留在系统应用名、窗口标题和 Bundle 身份中。
- 加速失败等连接问题优先在主页内展示可关闭提示，不再同时弹出重复错误弹窗；如果系统明确返回条件不允许，应尽快结束“加速中”状态并提示失败。
- 首页套餐区域先展示套餐名、每月额度和生效/到期日期，再展示本周期用量、已用与剩余。
- 中文节点地区使用 `English · 中文`，其他语言只显示英文名；公共规则版本面向用户称为“加速策略”。
- 使用熟悉的原生控件，并为默认、禁用、加载和错误状态提供完整反馈。
- 尊重用户已保存的数据和意图；停用不等于删除。
- 同时支持浅色、深色与多语言，并尽量减少对上游 Flutter 外壳的结构性改动。

## 无障碍与包容性

- 状态不能只依赖颜色表达，应同时提供文字或控件状态。
- 保持清晰的焦点、键盘和触控操作路径。
- 使用系统字体和常见 Material 控件，避免要求用户具备网络技术知识。
- 新增界面必须同时检查浅色、深色和窄窗口布局。

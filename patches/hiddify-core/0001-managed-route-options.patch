diff --git a/v2/config/builder.go b/v2/config/builder.go
index d37e8b6..363c01c 100644
--- a/v2/config/builder.go
+++ b/v2/config/builder.go
@@ -685,6 +685,10 @@ func setRoutingOptions(options *option.Options, hopt *HiddifyOptions) error {
 		)
 	}
 
+	// Product-level rules stay ahead of Hiddify's regional defaults so an
+	// explicit user choice can override a broader built-in match.
+	routeRules = append(routeRules, hopt.ManagedRouteRules...)
+
 	// for _, rule := range opt.Rules {
 	// 	routeRule := rule.MakeRule()
 	// 	switch rule.Outbound {
@@ -977,6 +981,19 @@ func setRoutingOptions(options *option.Options, hopt *HiddifyOptions) error {
 			},
 		})
 	}
+	// Reuse Hiddify rule sets when tags overlap, otherwise sing-box rejects the
+	// entire configuration as containing duplicate rule-set tags.
+	ruleSetTags := make(map[string]struct{}, len(rulesets)+len(hopt.ManagedRouteRuleSets))
+	for _, ruleSet := range rulesets {
+		ruleSetTags[ruleSet.Tag] = struct{}{}
+	}
+	for _, ruleSet := range hopt.ManagedRouteRuleSets {
+		if _, exists := ruleSetTags[ruleSet.Tag]; exists {
+			continue
+		}
+		rulesets = append(rulesets, ruleSet)
+		ruleSetTags[ruleSet.Tag] = struct{}{}
+	}
 	options.Route = &option.RouteOptions{
 		Rules:               routeRules,
 		Final:               OutboundMainDetour,
diff --git a/v2/config/hiddify_option.go b/v2/config/hiddify_option.go
index c3fa9bf..498ce83 100644
--- a/v2/config/hiddify_option.go
+++ b/v2/config/hiddify_option.go
@@ -23,12 +23,14 @@ type HiddifyOptions struct {
 	BalancerStrategy        string `json:"balancer-strategy,omitempty" overridable:"true"`
 	// GeoIPPath        string      `json:"geoip-path"`
 	// GeoSitePath      string      `json:"geosite-path"`
-	Rules     []Rule      `json:"rules,omitempty" overridable:"true"`
-	Warp      WarpOptions `json:"warp,omitempty"`
-	Warp2     WarpOptions `json:"warp2,omitempty"`
-	Mux       MuxOptions  `json:"mux,omitempty" overridable:"true"`
-	TLSTricks TLSTricks   `json:"tls-tricks,omitempty"`
-	EnableNTP bool        `json:"enable-ntp,omitempty"`
+	Rules                []Rule           `json:"rules,omitempty" overridable:"true"`
+	ManagedRouteRules    []option.Rule    `json:"managed-route-rules,omitempty"`
+	ManagedRouteRuleSets []option.RuleSet `json:"managed-route-rule-sets,omitempty"`
+	Warp                 WarpOptions      `json:"warp,omitempty"`
+	Warp2                WarpOptions      `json:"warp2,omitempty"`
+	Mux                  MuxOptions       `json:"mux,omitempty" overridable:"true"`
+	TLSTricks            TLSTricks        `json:"tls-tricks,omitempty"`
+	EnableNTP            bool             `json:"enable-ntp,omitempty"`
 
 	DNSOptions
 	InboundOptions
diff --git a/v2/config/managed_route_test.go b/v2/config/managed_route_test.go
new file mode 100644
index 0000000..d39699e
--- /dev/null
+++ b/v2/config/managed_route_test.go
@@ -0,0 +1,94 @@
+package config
+
+import (
+	"encoding/json"
+	"testing"
+
+	"github.com/sagernet/sing-box/option"
+)
+
+func TestManagedRoutesAreAddedWithoutReplacingHiddifyRouting(t *testing.T) {
+	settings := DefaultHiddifyOptions()
+	settings.Region = "cn"
+	err := json.Unmarshal([]byte(`{
+		"managed-route-rules": [{
+			"domain_suffix": ["rawya.ai"],
+			"action": "route",
+			"outbound": "direct §hide§"
+		}],
+		"managed-route-rule-sets": [{
+			"type": "remote",
+			"tag": "geosite-test",
+			"format": "binary",
+			"url": "https://rules.example/geosite-test.srs",
+			"download_detour": "nimbus-proxy",
+			"update_interval": "24h"
+		}, {
+			"type": "remote",
+			"tag": "geosite-cn",
+			"format": "binary",
+			"url": "https://rules.example/geosite-cn.srs",
+			"download_detour": "nimbus-proxy",
+			"update_interval": "24h"
+		}]
+	}`), settings)
+	if err != nil {
+		t.Fatalf("unmarshal managed routes: %v", err)
+	}
+
+	options := option.Options{DNS: &option.DNSOptions{}}
+	if err := setRoutingOptions(&options, settings); err != nil {
+		t.Fatalf("build routing options: %v", err)
+	}
+
+	routeJSON, err := json.Marshal(options.Route)
+	if err != nil {
+		t.Fatalf("marshal route options: %v", err)
+	}
+	var route map[string]any
+	if err := json.Unmarshal(routeJSON, &route); err != nil {
+		t.Fatalf("decode route options: %v", err)
+	}
+
+	rules, ok := route["rules"].([]any)
+	if !ok || len(rules) < 3 {
+		t.Fatalf("expected Hiddify setup rules and managed rule, got %v", route["rules"])
+	}
+	foundManagedRule := false
+	foundSniffRule := false
+	for _, rawRule := range rules {
+		rule, _ := rawRule.(map[string]any)
+		if rule["action"] == "sniff" {
+			foundSniffRule = true
+		}
+		if suffix, ok := rule["domain_suffix"].(string); ok && suffix == "rawya.ai" {
+			foundManagedRule = rule["outbound"] == "direct §hide§"
+		}
+	}
+	if !foundSniffRule {
+		t.Fatal("Hiddify sniff rule was replaced")
+	}
+	if !foundManagedRule {
+		t.Fatalf("managed rawya.ai direct rule was not added: %s", routeJSON)
+	}
+
+	ruleSets, ok := route["rule_set"].([]any)
+	if !ok || len(ruleSets) != 3 {
+		t.Fatalf("expected Hiddify country rule sets plus one managed rule set, got %v", route["rule_set"])
+	}
+	tagCounts := map[string]int{}
+	for _, rawRuleSet := range ruleSets {
+		ruleSet, _ := rawRuleSet.(map[string]any)
+		tag, _ := ruleSet["tag"].(string)
+		tagCounts[tag]++
+		if tag == "geosite-test" && ruleSet["download_detour"] != "nimbus-proxy" {
+			t.Fatalf("unexpected managed rule set: %v", ruleSet)
+		}
+	}
+	if tagCounts["geosite-cn"] != 1 {
+		t.Fatalf("expected Hiddify geosite-cn rule set to be reused, got %v", tagCounts)
+	}
+	if tagCounts["geosite-test"] != 1 {
+		t.Fatalf("expected non-overlapping managed rule set to be added, got %v", tagCounts)
+	}
+}
diff --git a/v2/hcore/hcore_service.proto b/v2/hcore/hcore_service.proto
index 7b48349..8eabb0b 100644
--- a/v2/hcore/hcore_service.proto
+++ b/v2/hcore/hcore_service.proto
@@ -16,7 +16,7 @@ service Core {
   rpc Setup (SetupRequest) returns (hcommon.Response);
   rpc Parse (ParseRequest) returns (ParseResponse);
   rpc ChangeHiddifySettings (ChangeHiddifySettingsRequest) returns (CoreInfoResponse);
-  //rpc GenerateConfig (GenerateConfigRequest) returns (GenerateConfigResponse);
+  rpc GenerateConfig (GenerateConfigRequest) returns (GenerateConfigResponse);
   rpc StartService (StartRequest) returns (CoreInfoResponse);
   rpc Stop (hcommon.Empty) returns (CoreInfoResponse);
   rpc Restart (StartRequest) returns (CoreInfoResponse);
@@ -30,4 +30,3 @@ service Core {
   rpc Close (CloseRequest) returns (hcommon.Empty); 
 }
 
-
diff --git a/v2/hcore/hcore_service_grpc.pb.go b/v2/hcore/hcore_service_grpc.pb.go
index c3d8911..032e82d 100644
--- a/v2/hcore/hcore_service_grpc.pb.go
+++ b/v2/hcore/hcore_service_grpc.pb.go
@@ -1,7 +1,7 @@
 // Code generated by protoc-gen-go-grpc. DO NOT EDIT.
 // versions:
 // - protoc-gen-go-grpc v1.5.1
-// - protoc             v5.28.0
+// - protoc             v6.33.0
 // source: v2/hcore/hcore_service.proto
 
 package hcore
@@ -29,6 +29,7 @@ const (
 	Core_Setup_FullMethodName                 = "/hcore.Core/Setup"
 	Core_Parse_FullMethodName                 = "/hcore.Core/Parse"
 	Core_ChangeHiddifySettings_FullMethodName = "/hcore.Core/ChangeHiddifySettings"
+	Core_GenerateConfig_FullMethodName        = "/hcore.Core/GenerateConfig"
 	Core_StartService_FullMethodName          = "/hcore.Core/StartService"
 	Core_Stop_FullMethodName                  = "/hcore.Core/Stop"
 	Core_Restart_FullMethodName               = "/hcore.Core/Restart"
@@ -55,7 +56,7 @@ type CoreClient interface {
 	Setup(ctx context.Context, in *SetupRequest, opts ...grpc.CallOption) (*hcommon.Response, error)
 	Parse(ctx context.Context, in *ParseRequest, opts ...grpc.CallOption) (*ParseResponse, error)
 	ChangeHiddifySettings(ctx context.Context, in *ChangeHiddifySettingsRequest, opts ...grpc.CallOption) (*CoreInfoResponse, error)
-	// rpc GenerateConfig (GenerateConfigRequest) returns (GenerateConfigResponse);
+	GenerateConfig(ctx context.Context, in *GenerateConfigRequest, opts ...grpc.CallOption) (*GenerateConfigResponse, error)
 	StartService(ctx context.Context, in *StartRequest, opts ...grpc.CallOption) (*CoreInfoResponse, error)
 	Stop(ctx context.Context, in *hcommon.Empty, opts ...grpc.CallOption) (*CoreInfoResponse, error)
 	Restart(ctx context.Context, in *StartRequest, opts ...grpc.CallOption) (*CoreInfoResponse, error)
@@ -203,6 +204,16 @@ func (c *coreClient) ChangeHiddifySettings(ctx context.Context, in *ChangeHiddif
 	return out, nil
 }
 
+func (c *coreClient) GenerateConfig(ctx context.Context, in *GenerateConfigRequest, opts ...grpc.CallOption) (*GenerateConfigResponse, error) {
+	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
+	out := new(GenerateConfigResponse)
+	err := c.cc.Invoke(ctx, Core_GenerateConfig_FullMethodName, in, out, cOpts...)
+	if err != nil {
+		return nil, err
+	}
+	return out, nil
+}
+
 func (c *coreClient) StartService(ctx context.Context, in *StartRequest, opts ...grpc.CallOption) (*CoreInfoResponse, error) {
 	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
 	out := new(CoreInfoResponse)
@@ -335,7 +346,7 @@ type CoreServer interface {
 	Setup(context.Context, *SetupRequest) (*hcommon.Response, error)
 	Parse(context.Context, *ParseRequest) (*ParseResponse, error)
 	ChangeHiddifySettings(context.Context, *ChangeHiddifySettingsRequest) (*CoreInfoResponse, error)
-	// rpc GenerateConfig (GenerateConfigRequest) returns (GenerateConfigResponse);
+	GenerateConfig(context.Context, *GenerateConfigRequest) (*GenerateConfigResponse, error)
 	StartService(context.Context, *StartRequest) (*CoreInfoResponse, error)
 	Stop(context.Context, *hcommon.Empty) (*CoreInfoResponse, error)
 	Restart(context.Context, *StartRequest) (*CoreInfoResponse, error)
@@ -384,6 +395,9 @@ func (UnimplementedCoreServer) Parse(context.Context, *ParseRequest) (*ParseResp
 func (UnimplementedCoreServer) ChangeHiddifySettings(context.Context, *ChangeHiddifySettingsRequest) (*CoreInfoResponse, error) {
 	return nil, status.Errorf(codes.Unimplemented, "method ChangeHiddifySettings not implemented")
 }
+func (UnimplementedCoreServer) GenerateConfig(context.Context, *GenerateConfigRequest) (*GenerateConfigResponse, error) {
+	return nil, status.Errorf(codes.Unimplemented, "method GenerateConfig not implemented")
+}
 func (UnimplementedCoreServer) StartService(context.Context, *StartRequest) (*CoreInfoResponse, error) {
 	return nil, status.Errorf(codes.Unimplemented, "method StartService not implemented")
 }
@@ -572,6 +586,24 @@ func _Core_ChangeHiddifySettings_Handler(srv interface{}, ctx context.Context, d
 	return interceptor(ctx, in, info, handler)
 }
 
+func _Core_GenerateConfig_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
+	in := new(GenerateConfigRequest)
+	if err := dec(in); err != nil {
+		return nil, err
+	}
+	if interceptor == nil {
+		return srv.(CoreServer).GenerateConfig(ctx, in)
+	}
+	info := &grpc.UnaryServerInfo{
+		Server:     srv,
+		FullMethod: Core_GenerateConfig_FullMethodName,
+	}
+	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
+		return srv.(CoreServer).GenerateConfig(ctx, req.(*GenerateConfigRequest))
+	}
+	return interceptor(ctx, in, info, handler)
+}
+
 func _Core_StartService_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
 	in := new(StartRequest)
 	if err := dec(in); err != nil {
@@ -790,6 +822,10 @@ var Core_ServiceDesc = grpc.ServiceDesc{
 			MethodName: "ChangeHiddifySettings",
 			Handler:    _Core_ChangeHiddifySettings_Handler,
 		},
+		{
+			MethodName: "GenerateConfig",
+			Handler:    _Core_GenerateConfig_Handler,
+		},
 		{
 			MethodName: "StartService",
 			Handler:    _Core_StartService_Handler,

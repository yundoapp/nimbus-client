# Yundo/Nimbus 客户端仓库协作补充规范

最后更新：`2026-07-15`

## App 修改后的必做流程

- 所有沟通、计划、评审、提交说明和代码注释优先使用简体中文。
- 只要修改会影响运行中 App 的 Flutter/Dart 源码、多语言、资源、依赖或 macOS 原生配置，必须在完成必要测试后执行 `scripts/build_install_run_macos_dev.sh`。
- 该脚本必须完成 `Yundo Dev` 构建、特权辅助进程校验、覆盖复制到 `/Applications/Yundo Dev.app`、重新启动和进程存活确认。任一环节失败时，任务不得宣布完成。
- 本机明确要求重新构建正式 `Yundo.app` 时，构建和校验成功后必须备份现有安装，完整覆盖 `/Applications/Yundo.app`，重新启动并确认进程存活；失败时恢复备份。该流程只用于本机正式版验证，不代表对外签名、公证或发布。
- 不能只检查源码、测试或 `build/` 目录中的 App；验收对象必须包含实际安装在 `/Applications` 的开发版。
- 这是本机 Debug 验收流程，不等于生成内测包、发布安装包或触发 release；纯文档或纯测试文件修改不触发该流程。
